import { View, Text } from 'react-native';
import React from 'react';
import { TouchableOpacity } from 'react-native-gesture-handler';

export default function rafcCompile() {
  console.log('first', first);
  return (
    <TouchableOpacity>
      <Text>rafcCompile</Text>

      <View />
    </TouchableOpacity>
  );
}
